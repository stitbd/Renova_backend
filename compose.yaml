services:
  app:
    build: .
    image: ${DOCKER_USERNAME}/renova-server:latest
    container_name: renova-server
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
